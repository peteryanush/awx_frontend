---
- name: Update instance
  host: all
  become: yes
  
  tasks:
  - name: Ping test
    ping:

  - name: Update IP
    include: network_manager.yml

  - name: Update Hostname
    include: awx-change-hostname.yml
